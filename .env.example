# Shared secret for the HTTP API — not a user login token.
# Send on every API call as X-API-Key (or Authorization: Bearer <key>).
# Comma-separate multiple keys. Override this in production; do not reuse the sample value.
WF_API_KEYS=local-dev-key
